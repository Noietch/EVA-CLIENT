"""Stateless utility helpers shared across the client — no session state.

Modules:
    math        rotation / quaternion conversions (Euler, wxyz, matrix)
    visualize   URDF scene + per-frame world transforms for the 3D canvas
    paths       eval output-dir resolution
    ssh_forward SSH local-forward + local port freeing for web eval
"""
