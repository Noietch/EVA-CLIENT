# EVA 数据审核与采集系统改造任务清单

> 交接对象：负责 EVA-CLIENT 数据审核、QC、Dashboard 和采集任务统一的开发同事。
> 目标：把当前数据审核页面改造成可快速打开、可连续质检、可导出补采任务，并与 EVA-CLIENT 主程序保持一致的审核工具。

## 1. 前因与背景

当前系统最初只是用于查看 task set 和采集数据，后来逐步增加了 QC、Dashboard、补采导出、机器人任务统一等功能。多次修改后出现了几个互相影响的问题：

- 页面切换批次或点击 slot 时会重新读取大量数据，响应很慢。
- QC 左侧方格的分页箭头和“下一条”复用了相近逻辑，实际点击时行为不正确。
- 50 个 slot 方格曾经出现长方形、重叠或无法占满卡片的问题。
- Dashboard 会跟随 QC 当前选择的机器人或批次，导致只显示一个机器人或局部数据。
- 机器人任务数量显示不一致，但业务要求所有机器人以 YAM 任务集为统一基准。
- 模板虽然已经移除部分按钮，但服务没有重启时浏览器仍会看到旧的“全部”按钮。
- 页面 Tab 编号缺失 `02`，与 EVA-CLIENT 主程序的 Tab 样式不一致。
- 页面中存在大量说明性小字和重复按钮，影响实际审核效率。
- 原始任务文件可读，但每次从任务文件和 LeRobot 数据重新计算状态成本较高，因此需要增加网站私有缓存。

历史上还处理过双 YAM 数据的相机频闪问题，包括 left/right wrist 和 high 相机的对比、降频闪、替换原数据等；这部分属于数据预处理历史，不应和本次网页改造混在一起，交接时只需确认原数据是否已按要求保留和替换。

## 2. 总体业务规则

1. YAM 是唯一的任务基准，其他机器人按照 YAM 的任务 ID、Scene ID、slot 数量和顺序生成对应 task set。
2. 不增加额外统一索引；每个机器人仍保留自己的标准 task set。
3. 任务文件仍是唯一可读、可编辑的真实来源。
4. 网站缓存只服务网页，不跟随 Hugging Face 上传。
5. 页面显示可以中文；状态枚举、数据库和导出文件统一使用英文值。
6. Dashboard 是全部数据的总览，不能受 QC 当前筛选项影响。
7. 当前最新展示要求：任务口径以 YAM 为准，前端不应把其他机器人变体展示成不同任务数量；底层原始数据不删除。

## 3. 任务目录标准化

每个机器人对应的 task set 目录必须包含：

- `info.yaml`
- `layout.yaml`
- `scene.csv`
- `tasks.csv`

需要检查并统一：

- TASK ID
- SCENE ID
- slot ID
- 每个任务的 slot 总数
- 任务排序
- 任务描述和中英文信息
- `robot_type`
- 任务文件与采集 episode 的 `slot_id` 关联

重点数据根目录：

```text
/mnt/dolphinfs/ssd_pool/docker/user/hadoop-aipnlp/EVA/yangheqing/datasets/data_collection/task_sets
```

## 4. 页面 Tab 和品牌

页面 Tab 固定为：

1. `01 Dashboard`
2. `02 Tasks / QC`
3. `03 Objects`

要求：

- 删除 Scene Tab。
- 恢复缺失的 `02` 编号。
- Tab 的尺寸、间距、激活态和 EVA-CLIENT 主程序保持一致。
- 顶部 EVA 图标使用主程序同一套环形 Logo。
- 浏览器标签页 favicon 也使用同一套 Logo。
- 不使用当前白色背景版本的 Logo。
- 语言通过启动参数控制，不增加页面语言切换按钮。
- 中文启动方式：

```bash
EVA_DATASET_LOCALE=zh
```

- 默认英文模式仍需保留。

## 5. QC 左侧面板

QC 页面左侧采用 EVA-CLIENT Collection 的布局：

### 5.1 三个步骤卡片

- `1 计划批次`
- `2 QC slot / 数据槽位`
- `3 质检操作`

当前代码中第二个卡片缺少步骤编号，需要补回 `2`。

### 5.2 批次选择

保留：

- 机器人选择
- 计划批次选择

“全部”按钮必须删除，包括第二张 QC 卡片底部残留的旧按钮。模板和 JS 事件都要清理，服务重启后再验证 DOM 中不存在该按钮。

### 5.3 slot 方格

- 每页固定 50 个 slot。
- 方格必须是正方形，不能使用固定最小高度导致长方形。
- 方格之间不能重叠。
- 50 个方格要完整显示，不挤压成不可读的小块。
- 左侧整个卡片区域允许上下滚动。
- 当前卡片高度要紧凑，不能产生大块空白。
- 每个 slot 的状态颜色统一：
  - pending：灰蓝
  - unreviewed：琥珀
  - passed：绿色
  - failed：红色

### 5.4 任务必要信息

必须保留并同时显示中英文必要信息：

- 批次名
- TASK 编号
- SC 编号
- slot 编号
- 当前选择的任务信息

删除无用的说明小字，例如：

- “点击编号进入质检 · 绿色通过 · 琥珀未质检……”
- 重复的状态说明
- 右侧重复任务标题
- 无人使用的说明性英文标题

## 6. 分页和“下一条”行为

这是当前最需要实际点击验收的部分。

### 6.1 左侧分页箭头

- 左箭头只切换上一页。
- 右箭头只切换下一页。
- 翻页不能自动打开某个 slot。
- 第一页左箭头禁用。
- 最后一页右箭头禁用。
- 当前选中的 slot 不得在重新渲染时强制把页面跳回原页。

### 6.2 QC 操作里的“下一条”

- “下一条”只打开下一条 slot。
- 当前页最后一条点击后，自动切换到下一页并打开第一页。
- 上一页第一条点击上一条时，自动回到上一页最后一条。
- 到达队列边界时按钮正确禁用。
- 不能只是修改 `qcPage` 而不调用 `openSlot`。

当前已发现的代码问题：

- `review.js` 的 `buildQcControls()` 调用了 `navigateQcSlot(1)`，但该函数没有在 `review.js` 中声明或通过 `configureReview()` 注入，实际点击会产生未定义错误。
- `renderTaskList()` 会根据当前 `selectedSlot` 重算 `qcPage`，可能覆盖手动翻页结果。
- 需要把“手动翻页”和“打开下一条”拆成两个独立行为。

## 7. QC 操作卡片

左侧“质检操作”卡片内部必须包含：

1. 下一条
2. 标记问题
3. 问题原因
4. 问题描述输入框
5. 保存
6. PASS
7. FAIL
8. 导出待补采清单

原因选项：

- `image_quality`
- `trajectory_quality`
- `task_mismatch`
- `other`

选择 `other` 时必须填写具体说明。

状态逻辑必须动态读取真实数据：

- 没有质检通过记录时显示 `0`。
- 点击 PASS 后数量自动增加。
- 不能在代码中永久写死为 0。
- QC 页面和 Dashboard 读取同一套真实状态。

右侧删除以下按钮和说明：

- 查看场景
- 查看任务
- 质检操作
- “确认当前数据状态并记录未通过原因”说明

## 8. Dashboard

### 8.1 数据范围

Dashboard 永远统计全部批次、全部数据，不受以下选择影响：

- 当前 QC 机器人
- 当前 QC 批次
- Scene 筛选
- TASK 筛选
- 当前 slot

当前代码的主要问题是：后端 `/api/state?robot_type=...` 返回的 `batches` 可能已经按机器人过滤，前端再从这个结果渲染 Dashboard，就会只看到一个机器人。解决方案应是：

- 后端始终返回全量批次摘要，例如 `all_batches`。
- QC 使用过滤后的批次和任务。
- Dashboard 使用 `all_batches` 和全局汇总。
- 缓存键需要包含版本号，避免旧缓存继续返回没有 `all_batches` 的旧结构。

### 8.2 采集需求图表

“采集需求 / 按机器人统计”不能再使用密集文字堆叠，应显示图表化卡片：

- 计划总量
- 已采集量
- 剩余量
- 计划批次数
- 横向进度条或堆叠进度条

任务数量以 YAM 任务集为准。当前最新要求是不要把其他机器人任务变体造成的数量差异单独展示出来；如果后续需要显示机器人对比，必须先保证任务口径完全统一。

### 8.3 质检健康度图表

使用状态图表或比例条展示：

- 已质检通过
- 未质检
- 质检未通过
- 待采集

标题必须和对应卡片贴合，卡片不得重叠、溢出或出现大量空白。

## 9. QC 报告与补采导出

1. QC 页面不要显示 Dashboard 的采集需求、机器人统计、流程健康度等汇总面板。
2. QC 报告只显示需要复核的实际数据。
3. 每条数据显示 QC 状态、原因和备注。
4. “导出待补采清单”放在左侧“质检操作”卡片中。
5. 没有选择批次时按钮可以显示，但必须不可用。
6. 导出同时生成：
   - CSV 清单
   - EVA-CLIENT 可直接读取的标准 task set 目录
7. 导出目录必须包含：
   - `info.yaml`
   - `layout.yaml`
   - `scene.csv`
   - `tasks.csv`
8. 导出内容只包含待采集和质检未通过的数据。
9. 状态、原因、备注的存储值使用英文。

## 10. 网站私有 SQLite 缓存

### 10.1 设计原则

- SQLite 是网站缓存，不是数据集。
- 缓存文件不能放入 Hugging Face 上传目录。
- 任务文件仍是真实来源。
- 缓存失效不能导致任务文件被覆盖或丢失。

### 10.2 缓存内容

可缓存：

- 批次摘要
- 批次任务状态
- slot 状态
- QC 统计
- QC 详情
- 文件修改时间
- 页面需要的索引数据

不应缓存为唯一来源：

- 原始 YAML/CSV 内容
- 原始视频
- 需要上传到 Hugging Face 的数据

### 10.3 更新策略

页面加载时：

1. 先读 SQLite，快速显示 Dashboard 和批次列表。
2. 后台检查任务文件修改时间。
3. 文件有变化时只刷新对应批次。
4. 点击已经访问过的批次时优先命中缓存。
5. QC 详情短时间缓存，避免重复点击同一 slot 重复请求。
6. 修改计划、导入计划、编辑 QC 后清理对应批次缓存。
7. 缓存键必须带版本号，防止旧结构缓存污染新接口。

当前代码中已有前端 `stateCache` 和 `reviewCache`，后端 SQLite 状态缓存也有初步实现，但需要完成真实运行验证：

- 服务首次启动能正常创建缓存。
- 缓存命中时 `/api/state` 响应明显变快。
- 文件发生变化后能刷新。
- QC 写入后缓存不会显示旧状态。
- SQLite 文件不会出现在数据导出 ZIP 中。

## 11. 服务启动和运行

服务地址：

```text
http://0.0.0.0:8416/
```

中文启动：

```bash
EVA_DATASET_PORT=8416 EVA_DATASET_LOCALE=zh tools/datasets/start.sh
```

每次修改模板、JS、CSS 或后端后都必须重启服务。服务刚启动时可能短暂返回 503，必须等初始化完成后再验收，不能看到启动日志就直接宣称完成。

## 12. 必须执行的验收

### 静态检查

```bash
node --check tools/datasets/static/editor.js
node --check tools/datasets/static/entity-ui.js
node --check tools/datasets/static/review.js
.venv/bin/python -m py_compile tools/datasets/app.py tools/datasets/collection.py
.venv/bin/ruff check tools/datasets/app.py tools/datasets/collection.py tools/datasets/static
 git diff --check
```

### 接口检查

确认以下接口可用：

- `/`
- `/api/state`
- `/api/state?batch=<batch>`
- `/api/state?robot_type=<robot>`
- `/api/review?batch=<batch>&slot_id=<slot>`
- `/api/batches/<batch>/qc/export`

确认：

- 首页 HTTP 200。
- `/api/state` 能返回全局批次和机器人摘要。
- Dashboard 使用全量数据。
- QC 使用当前批次数据。
- 导出 ZIP 只包含标准四个文件。

### 真实页面点击检查

必须在真实浏览器中验证：

1. 页面 Tab 显示 `01 / 02 / 03`。
2. 页面上不存在“全部”按钮。
3. 选择批次后能看到 50 个正方形 slot。
4. 左箭头只翻页。
5. 右箭头只翻页。
6. QC“下一条”打开下一条数据。
7. 跨页后自动打开下一页第一条。
8. PASS/FAIL 能改变状态颜色和统计数量。
9. Dashboard 不随 QC 选择变化。
10. Dashboard 能显示正确的 YAM 任务统计。
11. 重新点击已访问批次时命中缓存，页面不出现长时间白屏。
12. 文件更新后刷新能看到新状态。
13. 导出补采清单可被 EVA-CLIENT 读取。

## 13. 交接时的当前风险点

1. 需要重启服务才能看到模板删除效果；旧服务进程会继续提供旧 HTML。
2. `review.js` 中“下一条”当前需要确认 `navigateQcSlot` 是否正确注入。
3. `renderTaskList()` 的选中 slot 自动分页逻辑可能覆盖手动翻页。
4. Dashboard 不能直接使用按机器人过滤后的 `app.state.batches`。
5. SQLite 缓存需要版本化，否则旧缓存结构可能继续导致 Dashboard 只显示一个机器人。
6. 浏览器若缺少系统图形依赖，不能把“无法截图”当作页面功能已验收；应使用可运行的浏览器环境完成点击验证。
7. 不能只做语法检查就交付，分页、下一条、PASS/FAIL 和 Dashboard 全局统计必须真实操作确认。

## 14. 交付标准

只有以下条件全部满足，才能宣布完成：

- 任务集以 YAM 为准完成统一。
- Dashboard 是全局总览。
- QC 页面布局与 EVA-CLIENT Collection 一致。
- 50 格正方形分页正常。
- 左侧翻页与“下一条”行为完全分离且可点击验证。
- “全部”按钮和无用说明已删除。
- Tab、Logo、favicon 与主程序一致。
- QC 状态动态更新，初始无通过记录时为 0，后续可增长。
- 补采导出可被 EVA-CLIENT 直接读取。
- SQLite 只作为网站私有缓存，不进入 Hugging Face 数据集。
- 服务重启后接口和页面均通过真实验收。
